package com.mootly.wcm.model;

/**
 * <ResidentialStatus> xs:string (value comes from list: {'RES'|'NRI'|'NOR'}) (length <= 3) </ResidentialStatus> [1]
 * @author admin
 *
 */
public enum ResidentStatus {
	RES,
	NRI,
	NOR
}
